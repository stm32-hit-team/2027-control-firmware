# 读卡到播报

这一页讲一张卡从靠近到被念出来的全过程。

## 状态机

读卡流程在库 `lib/rfid_core` 里。应用层只在 [[src-app.c]] 里接结果。

```mermaid
stateDiagram-v2
    [*] --> IDLE
    IDLE --> WAIT_UID: 到点发寻卡
    WAIT_UID --> IDLE: 无卡或超时
    WAIT_UID --> WAIT_BLOCK: 读到 UID
    WAIT_BLOCK --> WAIT_BLOCK: 读下一块
    WAIT_BLOCK --> WAIT_DISPATCH: 文字拼好
    WAIT_DISPATCH --> WAIT_REMOVAL: 语音收下
    WAIT_DISPATCH --> WAIT_DISPATCH: 队列满，稍后重试
    WAIT_REMOVAL --> WAIT_REMOVAL: 卡还在
    WAIT_REMOVAL --> IDLE: 确认拿走
```

## 一张卡的时间线

```mermaid
sequenceDiagram
    participant M as 单片机
    participant R as 读卡器
    participant T as 语音

    M->>R: 寻卡命令
    R-->>M: 有卡，回 UID
    M->>R: 读数据块
    R-->>M: 回文字
    M->>T: 文字入队
    M->>M: 点亮 PA8
    T-->>M: 按字数估算念多久
    M->>R: 反复确认卡还在
    R-->>M: 连续几次无卡
    M->>M: 允许下次再念
```

## 关键规则

- 同一张卡一直放着，只念一次。靠 UID 判重。
- 拿走再放回，会再念一次。靠连续几次读不到来确认。
- 队列满时，这张卡保住，过一会儿再送。不会丢。
- 非法 GB2312 不会送给语音。

## 和代码的关系

- 事件怎么进语音队列：[[src-app.c]] 的 `app_on_rfid_event`
- 串口字节怎么进来：[[src-board_stm32.c]] 的 `HAL_UART_RxCpltCallback`
- 读卡节奏在哪调：[[include-app_config.h]]

## 下一步

- 每个函数做什么：[[04-函数索引]]
- 出问题怎么查：[[06-故障排查]]
